# Software

This directory contains the firmware/software source code.

## Prerequisites

*List prerequisites to build project*
- [Tool 1] (e.g., CMake 3.20+)
- [Tool 2] (e.g., arm-none-eabi-gcc)

## Building

*Example build command snippet for a CMake project*
```bash
cmake -B build
cmake --build build
```

## Structure

- `src/`: Source files.
- `CMakeLists.txt`: Build configuration.
